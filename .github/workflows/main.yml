name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "37 18 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@master
